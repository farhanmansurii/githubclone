import React from "react";
const Loading = () => (
  <div className="loading">
    <img src={require('../images/spinner.gif')} className="w-40 m-60" alt="SVG as an image"/>

  </div>
);

export default Loading;